= simple_form_for(@user) do |f|
  .row
    .col-md-3
      = f.input :name
      = f.input :email
      = f.input :password
      = f.input :password_confirmation
  .row
    %hr
    .col-md-12
      = f.button :submit, class: 'btn-primary'
